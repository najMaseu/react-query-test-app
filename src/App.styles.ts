import { css } from "@emotion/react";

export const appStyles = css(
    {
      width: '100vw',
      height: '100vh',
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
      flexDirection : 'column'
    }
  )